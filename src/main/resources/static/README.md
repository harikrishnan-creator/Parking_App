# Frontend served by Spring Boot

The static frontend is copied into src/main/resources/static so Spring Boot can serve the UI and API from the same origin.

Access the app at: http://localhost:8080/
